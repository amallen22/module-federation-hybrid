{
  "name": "cv-hibrid",
  "private": true,
  "dependencies": {
    "vitest": "^3.1.3"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter shell --filter product --filter ui --filter login dev",
    "dev:minimal": "pnpm --parallel --filter shell --filter product --filter ui --filter login dev",
    "build:all": "pnpm --filter ui build && pnpm --filter product build && pnpm --filter shell build && pnpm --filter user build",
    "preview:all": "pnpm --parallel --filter shell --filter product --filter ui --filter user preview",
    "dev:build-preview": "pnpm build:all && pnpm preview:all"
  }
}
